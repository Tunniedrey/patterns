class phoneNumber {
    constructor (AddPhoneNumber,RemoviePhoneNumber,DialPhoneNumber) {
        this.observers = new set ()
    }
    add(AddPhoneNumber) {
        this.AddPhoneNumber.add(090112233);
        this.AddPhoneNumber.add(080223344);
        this.AddPhoneNumber.add(070334455);
    }
    Remove(RemoviePhoneNumber) {
        this.RemoviePhoneNumber.delete()
    }
    dial(DialPhoneNumber) {
        this.DialPhoneNumber.call()
    }
    notify(DialPhoneNumber) {
        for(let observers of this.DialPhoneNumber);
    }

    const = new Notification ('incoming call');

    
}